import { forwardRef } from "react";

const KbTable = forwardRef(({
    filters = {},
    columns = [],
    defaultSorting = {
        column: '',
        direction: '',
    },
    fetchData,
    actions
    
}, ref)=>{
    return(
        <>
        
        </>
    )
})
export default KbTable